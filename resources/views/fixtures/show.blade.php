@php
    $matchName = $fixture->homeTeam->name.' x '.$fixture->awayTeam->name;
    $channels = $fixture->channels->pluck('name')->join(', ', ' e ');
    $dateLabel = $fixture->starts_at->translatedFormat('l, d \\d\\e F \\d\\e Y');
    $title = $matchName.': onde assistir ao vivo e horário';
    $description = $matchName.' pela '.$fixture->competition->name.' em '.$fixture->starts_at->format('d/m/Y').' às '.$fixture->starts_at->format('H:i').'. Veja onde assistir ao vivo: '.$channels.'.';
    $schema = ['@context'=>'https://schema.org','@type'=>'SportsEvent','name'=>$matchName,'startDate'=>$fixture->starts_at->toIso8601String(),'eventStatus'=>'https://schema.org/EventScheduled','url'=>$fixture->publicUrl(),'description'=>$description,'homeTeam'=>['@type'=>'SportsTeam','name'=>$fixture->homeTeam->name],'awayTeam'=>['@type'=>'SportsTeam','name'=>$fixture->awayTeam->name],'competitor'=>[['@type'=>'SportsTeam','name'=>$fixture->homeTeam->name],['@type'=>'SportsTeam','name'=>$fixture->awayTeam->name]]];
@endphp
<!DOCTYPE html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#0756b9"><title>{{ $title }} | Futebol na TV</title><meta name="description" content="{{ $description }}"><link rel="canonical" href="{{ $fixture->publicUrl() }}"><meta property="og:type" content="article"><meta property="og:title" content="{{ $title }}"><meta property="og:description" content="{{ $description }}"><meta property="og:url" content="{{ $fixture->publicUrl() }}"><meta name="twitter:card" content="summary"><script type="application/ld+json">{!! json_encode($schema, JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES) !!}</script>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-[#f4f7fb] text-slate-950 antialiased"><header class="brand-header text-white"><div class="relative z-10 mx-auto max-w-5xl px-4 py-5 sm:px-6"><a href="{{ route('home') }}" class="flex items-center gap-3"><span class="grid h-11 w-11 place-items-center rounded-2xl border border-white/20 bg-white/15">@include('partials.ball-icon')</span><span><strong class="block text-xl font-black">Futebol na TV</strong><small class="text-blue-100">Seu guia de transmissões ao vivo</small></span></a></div></header>
<main class="mx-auto max-w-5xl px-4 py-8 sm:px-6 sm:py-12"><nav class="mb-6 text-sm font-bold text-blue-700"><a href="{{ route('home') }}">Início</a> <span class="mx-2 text-slate-300">/</span> <a href="{{ route('fixtures.by-date',['date'=>$fixture->starts_at->format('Y-m-d')]) }}">Jogos de {{ $fixture->starts_at->format('d/m') }}</a></nav>
<article><header class="mb-7"><p class="text-sm font-extrabold uppercase tracking-widest text-blue-700">{{ $fixture->competition->name }}</p><h1 class="mt-2 text-3xl font-black tracking-[-.04em] sm:text-5xl">{{ $matchName }}: onde assistir ao vivo</h1><p class="mt-4 text-lg text-slate-600">Confira horário, transmissão e todas as informações da partida.</p></header>
<section class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-xl shadow-slate-900/[.07]"><div class="border-b border-slate-100 bg-slate-50 px-5 py-4 text-center text-sm font-extrabold capitalize text-slate-600">{{ $dateLabel }} · {{ $fixture->starts_at->format('H:i') }} (horário de Brasília)</div>
<div class="grid grid-cols-[1fr_auto_1fr] items-center gap-3 px-4 py-9 sm:gap-8 sm:px-10"><a href="{{ $fixture->homeTeam->publicUrl() }}" class="group text-center">@if($fixture->homeTeam->logoSource())<img src="{{ $fixture->homeTeam->logoSource() }}" alt="Escudo {{ $fixture->homeTeam->name }}" class="mx-auto h-20 w-20 object-contain transition group-hover:scale-105 sm:h-28 sm:w-28">@endif<h2 class="mt-4 text-base font-black group-hover:text-blue-700 group-hover:underline sm:text-xl">{{ $fixture->homeTeam->name }}</h2></a><span class="rounded-full bg-blue-50 px-3 py-2 text-sm font-black text-blue-700">X</span><a href="{{ $fixture->awayTeam->publicUrl() }}" class="group text-center">@if($fixture->awayTeam->logoSource())<img src="{{ $fixture->awayTeam->logoSource() }}" alt="Escudo {{ $fixture->awayTeam->name }}" class="mx-auto h-20 w-20 object-contain transition group-hover:scale-105 sm:h-28 sm:w-28">@endif<h2 class="mt-4 text-base font-black group-hover:text-blue-700 group-hover:underline sm:text-xl">{{ $fixture->awayTeam->name }}</h2></a></div>
<div class="border-t border-blue-100 bg-blue-50/70 px-5 py-5 text-center"><p class="text-xs font-extrabold uppercase tracking-widest text-blue-600">Onde assistir ao vivo</p><div class="mt-3 flex flex-wrap justify-center gap-2">@foreach($fixture->channels as $channel)<a href="{{ $channel->publicUrl() }}" class="channel-pill rounded-xl bg-white px-4 py-2 text-sm font-extrabold text-blue-800 hover:bg-blue-100 hover:underline">▻ {{ $channel->name }}</a>@endforeach</div></div></section>
<div class="mt-8 grid gap-6 lg:grid-cols-[1fr_18rem]"><section class="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><h2 class="text-2xl font-black">{{ $matchName }}: informações da partida</h2><p class="mt-4 leading-7 text-slate-600"><a href="{{ $fixture->homeTeam->publicUrl() }}" class="font-bold text-blue-700 hover:underline">{{ $fixture->homeTeam->name }}</a> e <a href="{{ $fixture->awayTeam->publicUrl() }}" class="font-bold text-blue-700 hover:underline">{{ $fixture->awayTeam->name }}</a> se enfrentam pela competição {{ $fixture->competition->name }}. A partida está marcada para {{ $dateLabel }}, às {{ $fixture->starts_at->format('H:i') }}, no horário de Brasília.</p><h2 class="mt-7 text-xl font-black">Onde assistir {{ $matchName }}?</h2><p class="mt-3 leading-7 text-slate-600">A transmissão ao vivo está anunciada em {{ $channels }}. A grade das emissoras pode mudar sem aviso prévio, por isso recomendamos conferir a programação perto do início do jogo.</p><h2 class="mt-7 text-xl font-black">Que horas começa?</h2><p class="mt-3 leading-7 text-slate-600">O jogo começa às <strong>{{ $fixture->starts_at->format('H:i') }}</strong> desta {{ $fixture->starts_at->translatedFormat('l') }}, pelo horário de Brasília.</p></section>
<aside class="rounded-2xl border border-slate-200 bg-white p-6"><h2 class="font-black">Resumo</h2><dl class="mt-4 space-y-4 text-sm"><div><dt class="text-slate-500">Campeonato</dt><dd class="font-bold">{{ $fixture->competition->name }}</dd></div><div><dt class="text-slate-500">Data</dt><dd class="font-bold">{{ $fixture->starts_at->format('d/m/Y') }}</dd></div><div><dt class="text-slate-500">Horário</dt><dd class="font-bold">{{ $fixture->starts_at->format('H:i') }}</dd></div><div><dt class="text-slate-500">Transmissão</dt><dd class="font-bold">{{ $channels }}</dd></div></dl></aside></div>
@if($relatedFixtures->isNotEmpty())<section class="mt-8"><h2 class="mb-4 text-xl font-black">Mais jogos de {{ $fixture->competition->name }}</h2><div class="grid gap-3 sm:grid-cols-2">@foreach($relatedFixtures as $related)<div class="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><p class="text-xs font-bold text-blue-700">{{ $related->starts_at->format('d/m · H:i') }}</p><p class="mt-1 font-black"><a href="{{ $related->homeTeam->publicUrl() }}" class="hover:text-blue-700 hover:underline">{{ $related->homeTeam->name }}</a> <span class="text-slate-300">x</span> <a href="{{ $related->awayTeam->publicUrl() }}" class="hover:text-blue-700 hover:underline">{{ $related->awayTeam->name }}</a></p><a href="{{ $related->publicUrl() }}" class="mt-2 inline-block text-xs font-extrabold text-blue-600">Ver partida →</a></div>@endforeach</div></section>@endif
</article><footer class="mt-10 border-t border-slate-200 pt-6 text-sm text-slate-500">Informações de transmissão sujeitas a alteração pelos canais.</footer></main></body></html>
