<!DOCTYPE html><html lang="pt-BR"><head>@include('partials.google-tag-head')<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Prioridade dos campeonatos | Administração</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-[#f4f7fb] text-slate-950">@include('partials.google-tag-body')
<header class="bg-slate-950 text-white"><div class="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-4 px-4 py-4 sm:px-6"><div><strong class="block text-lg font-black">Futebol na TV</strong><small class="text-slate-400">Administração</small></div><nav class="flex items-center gap-1 text-sm font-bold"><a href="{{ route('admin.channels.index') }}" class="rounded-lg px-3 py-2 hover:bg-white/10">Canais</a><a href="{{ route('admin.competitions.index') }}" class="rounded-lg bg-white/10 px-3 py-2">Campeonatos</a><form method="post" action="{{ route('admin.logout') }}">@csrf<button class="rounded-lg px-3 py-2 hover:bg-white/10">Sair</button></form></nav></div></header>
<main class="mx-auto max-w-6xl px-4 py-8 sm:px-6"><div class="mb-7 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-xs font-extrabold uppercase tracking-widest text-blue-700">Ordem da programação</p><h1 class="mt-1 text-3xl font-black">Prioridade dos campeonatos</h1><p class="mt-2 text-slate-500">Quanto menor o número, mais acima a liga aparece na lista de jogos.</p></div><form method="get" class="flex gap-2"><input name="busca" value="{{ $search }}" placeholder="Buscar campeonato" class="min-w-0 rounded-xl border border-slate-300 px-4 py-2.5 outline-none"><button class="rounded-xl bg-slate-900 px-4 py-2.5 font-bold text-white">Buscar</button></form></div>
@if(session('status'))<div class="mb-5 rounded-xl bg-emerald-50 px-4 py-3 font-bold text-emerald-800">{{ session('status') }}</div>@endif
<div class="space-y-3">@foreach($competitions as $competition)<form method="post" action="{{ route('admin.competitions.update', $competition) }}" class="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-[1fr_8rem_auto] sm:items-center">@csrf @method('PUT')<div><a href="{{ $competition->publicUrl() }}" target="_blank" class="font-black hover:text-blue-700">{{ $competition->name }} ↗</a><p class="text-xs text-slate-400">{{ $competition->fixtures_count }} jogos cadastrados</p></div><div><label for="priority-{{ $competition->id }}" class="mb-1 block text-xs font-bold text-slate-500 sm:sr-only">Prioridade</label><input id="priority-{{ $competition->id }}" name="display_priority" type="number" min="1" max="9999" value="{{ $competition->display_priority }}" class="w-full rounded-xl border border-slate-300 px-4 py-2.5 text-center text-sm font-black outline-none focus:border-blue-500"></div><button class="rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-extrabold text-white">Salvar</button></form>@endforeach</div>
<div class="mt-8">{{ $competitions->links() }}</div></main></body></html>
